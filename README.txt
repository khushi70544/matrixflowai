MatrixFlowAI V5
Replace dashboard.html and worker.js.

Cloudflare Worker secrets:
OPENROUTER_API_KEY = existing key

Optional:
FAL_KEY
HF_TOKEN

Note: Video provider/model availability, pricing and native resolution depend on the provider. The UI exposes 4K/8K workflow targets but does not guarantee free native 8K generation.
